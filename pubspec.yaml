name: cubit_pool
description: "A new Flutter package project."
version: 0.0.2
homepage: null

environment:
  sdk: '>=3.3.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.5
  hydrated_bloc: ^9.1.5
  logging: ^1.2.0

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter: null
