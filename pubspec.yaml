name: cubit_pool
description: "A new Flutter package project."
version: 1.0.2
homepage: null

environment:
  sdk: ">=3.7.2 <4.0.0"

dependencies:
  cloud_firestore: ^5.6.0
  firebase_auth: ^5.3.4
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.0
  hydrated_bloc: ^10.0.0
  logging: ^1.2.0

dev_dependencies:
  equatable: ^2.0.7
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4

flutter: null
